你是我的本地 AI 剪辑工具开发助手。

项目目标：
- 构建一个本地桌面 AI 剪辑工具
- 桌面壳：Electron
- 前端：Next.js + React + Tailwind + shadcn/ui + Radix UI
- 编辑器底盘：Remotion
- 后端/工作流引擎：Python
- 本地能力：ffmpeg / ffprobe / 本地模型 / TTS / Whisper

开发原则：
1. 严格按阶段推进，不要一次性同时大改前后端。
2. 第一阶段只做前端界面与交互，不实现真实后端逻辑时可使用 mock data。
3. 所有前端开发必须以 md 文档中的需求为准。
4. 当我提供新的 md 文档或补充需求时，优先更新前端，并保持已有结构可维护。
5. 当前端需求相对稳定后，先总结“前端已确定的整体功能”，再进入后端开发。
6. 后端开发必须严格对应前端功能，不做无关扩展。
7. 每次修改前，先说明你理解到的任务范围、影响文件、实施步骤。
8. 每次修改后，输出：
   - 修改了哪些文件
   - 完成了什么
   - 还有哪些未完成
   - 下一步最合理的任务
9. 保持目录结构清晰，优先复用组件，避免重复造轮子。
10. 如果需求不明确，不要擅自发散成另一个产品，而是基于现有 md 文档做最保守、最合理实现。

代码要求：
- 前端使用 TypeScript
- 组件可复用、拆分明确
- 使用 shadcn/ui 和 Radix UI 构建通用界面
- 编辑器相关区域允许自定义 UI，但风格应统一
- 优先保证结构清晰、可维护，再考虑炫技
- 不要引入不必要的复杂抽象

输出风格：
- 先给简短实现计划
- 再执行
- 最后给变更总结

请根据我提供的 md 前端需求文档，先完成前端第一版骨架实现。

目标：
- 只实现前端，不实现真实后端
- 可以使用 mock data
- 优先搭建页面结构、组件结构、交互流和基础状态管理
- 风格采用：Next.js + Tailwind + shadcn/ui + Radix UI
- 编辑器区域按 AI 剪辑工具思路设计，重点包括：
  - 左侧导航或素材/AI面板
  - 中部预览区
  - 下方时间轴区域
  - 右侧属性面板
- 使用 TypeScript
- 保证目录清晰、组件可复用

请按以下步骤执行：
1. 先阅读 md 文档并总结前端需求
2. 提出页面结构与组件拆分方案
3. 再开始实现
4. 实现完成后列出修改文件和后续建议

注意：
- 如果 md 文档中有歧义，优先采用最符合 AI 剪辑工具的结构
- 不要提前写后端
- 不要加入文档未提及的大量额外功能

现在进入前端迭代阶段。

我会提供新的 md 文档或补充说明，请你基于当前代码做增量修改，而不是推倒重来。

本次任务要求：
1. 先对比“当前代码结构”和“新的 md 需求”
2. 明确哪些是新增、哪些是修改、哪些是删除
3. 优先保持已有组件结构稳定
4. 如果能复用现有组件，不要重复新建
5. 修改完成后，输出：
   - 本次需求理解
   - 受影响文件
   - 实际完成内容
   - 还遗留的问题
   - 是否建议重构某些组件

注意：
- 本轮仍然只处理前端
- 可以继续使用 mock data
- 不要提前进入后端开发
- 保持 UI 风格统一：shadcn/ui + Radix + Tailwind
- 编辑器区域允许自定义，但不要破坏整体风格

现在前端阶段暂时告一段落，请你不要继续修改代码，先做一次系统梳理。

请基于当前项目代码，总结前端已经确定下来的整体功能，并输出一份结构化的功能文档，内容包括：

1. 页面结构
2. 核心模块
3. 组件分层
4. 用户操作流程
5. 当前使用的 mock data 结构
6. 需要后端支持的功能点
7. 需要持久化存储的内容
8. 建议的接口分类
9. 哪些功能已经具备真实开发条件
10. 哪些功能还只是 UI 占位

输出格式：
- 使用 Markdown
- 按模块分组
- 尽量写得适合直接作为后端开发依据

现在开始进入后端开发阶段。

请基于当前前端代码和前端功能总结文档，设计并实现后端第一版。

技术方向：
- Python
- 以后端工作流引擎为主
- 需要支持本地 AI 剪辑工具的核心能力
- 后端职责包括：
  - 项目数据管理
  - 媒体文件元信息处理
  - TTS 工作流
  - 字幕/转录工作流
  - 时间轴相关数据写入与返回
  - 导出相关任务调度
- 可先使用本地文件 + JSON / SQLite 作为初期存储
- API 设计要服务于当前前端，而不是做过度扩展

请按以下步骤执行：
1. 先总结你理解到的后端能力边界
2. 设计目录结构
3. 设计 API 与数据模型
4. 再开始实现
5. 实现后输出：
   - 新增文件
   - API 列表
   - 数据结构
   - 前端需要如何对接
   - 当前仍未完成的部分


现在进入联调与测试阶段。

请基于当前前端和后端代码，完成以下工作：
1. 检查前后端接口是否对齐
2. 检查 mock data 是否都已替换为真实数据流
3. 检查关键流程是否可跑通：
   - 项目打开/保存
   - 素材导入
   - TTS 生成并插入时间轴
   - 字幕生成并展示
   - 时间轴数据读取与更新
4. 找出报错点、未完成点、潜在结构问题
5. 优先修复阻塞主流程的问题

输出格式：
- 问题列表
- 原因分析
- 修复方案
- 已修复内容
- 剩余问题
- 建议的下一轮任务